using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShovelScript : MonoBehaviour
{
    //float y = 0;

    //// Update is called once per frame
    //void Update()
    //{
    //    y += Time.deltaTime;
    //    transform.localPosition = new Vector3(0, Mathf.Sin(y), 0); ;
    //}
}
